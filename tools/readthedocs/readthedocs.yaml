# readthedocs yaml config file version
version: 2

build:
    image: latest

sphinx:
    builder: html
    configuration: ./sphinx_api/conf.py
    fail_on_warning: false

python:
    version: 3.7
  
conda:
    environment: ./tools/readthedocs/environment.yaml
