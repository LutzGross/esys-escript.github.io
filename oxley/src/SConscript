##############################################################################
#
# Copyright (c) 2003-2026 by the esys.escript Group
# https://github.com/LutzGross/esys-escript.github.io
#
# Primary Business: Queensland, Australia
# Licensed under the Apache License, version 2.0
# http://www.apache.org/licenses/LICENSE-2.0
#
# See CREDITS file for contributors and development history
#
##############################################################################

Import('*')

module_name = 'oxley'

sources = """
    Brick.cpp
    domainhelpers.cpp
    FinleyConverter.cpp
    InitAlgorithms.cpp
    Rectangle.cpp
    OtherAlgorithms.cpp
    OxleyData.cpp
    OxleyDomain.cpp
    MeshIO.cpp
    RefinementAlgorithms.cpp
    RefinementType.cpp
    RefinementQueue.cpp
    tictoc.cpp
""".split()

headers = """
    AbstractAssembler.h
    Brick.h
    DefaultAssembler2D.h
    DefaultAssembler3D.h
    domainhelpers.h
    FinleyConverter.h
    InitAlgorithms.h
    MeshAccess.h
    Rectangle.h
    system_dep.h
    OtherAlgorithms.h
    Oxley.h
    OxleyData.h
    OxleyDomain.h
    OxleyException.h
    MeshIO.h
    RefinementAlgorithms.h
    RefinementType.h
    RefinementQueue.h
    tictoc.h
""".split()

# The assembler is trilinos-only: it builds the matrix through
# trilinoswrap, and there is no paso path for it. It is also the one part of
# oxley that the current design does not need - an adaptive oxley mesh is
# solved by exporting it to finley - so a build without trilinos leaves it out
# rather than shipping an oxley that cannot be imported. createAssembler
# throws in that case, naming toFinley().
if env['trilinos']:
    sources += """
        DefaultAssembler2D.cpp
        DefaultAssembler3D.cpp
    """.split()

local_env = env.Clone()

# collect dependencies for other modules
oxleylibs = []
oxleylibs += env['escript_libs']
oxleylibs += env['p4est_libs']
# oxley hands its mesh over to finley for degrees of freedom and assembly
oxleylibs += env['finley_libs']
if env['paso']:
    oxleylibs += env['paso_libs']
if env['trilinos']:
    oxleylibs += env['trilinoswrap_libs']
if env['compressed_files']:
    oxleylibs += env['compression_libs']
if env['silo']:
    oxleylibs += env['silo_libs']

#p4est library
import os
local_env.AppendUnique(CPPPATH = [os.path.join(env['escript_src'], 'p4est', 'src')])

local_env.PrependUnique(LIBS = oxleylibs)


include_path = Dir(module_name, local_env['incinstall'])
hdr_inst = local_env.Install(include_path, headers)

lib = local_env.SharedLibrary(module_name, sources)
lib_inst = local_env.Install(local_env['libinstall'], lib)

if IS_OSX:
    env['oxley_libs'] = [lib_inst] + oxleylibs
else:
    env['oxley_libs'] = [module_name] + oxleylibs

### Python wrapper ###
py_env = local_env.Clone()
if IS_OSX:
    py_env.PrependUnique(LIBS = [lib_inst])
else:
    py_env.PrependUnique(LIBS = [module_name])
py_lib_name = module_name + 'cpp'
py_lib = py_env.PythonModule(py_lib_name, 'oxleycpp.cpp')

mod_path = Dir(module_name, local_env['pyinstall'])
mod_inst = py_env.Install(mod_path, py_lib)

build = env.Alias('build_oxley', [hdr_inst, lib, py_lib])
env.Alias('install_oxley', ['build_oxley', lib_inst, mod_inst])
