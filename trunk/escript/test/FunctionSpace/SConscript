# Scons configuration file for FunctionSpace unit tests

#
# set appropriate defaults for configuration variables
esysroot=str(Dir('./../../../..').abspath)
execfile(str(File(esysroot+"/scons/esys_options.py")))

cxx_flags=cxx_flags_debug

print "Build configuration for module: FunctionSpace unit tests"
print "             cxx: ", cxx
print "       cxx_flags: ", cxx_flags

#
# do the actual build

functionspace_test_path = str(esysroot) + '/escript/test/FunctionSpace'
escript_path = str(esysroot) + '/escript/inc'
esysUtils_path = str(esysroot) + '/esysUtils/src'
CppUnitTest_path = str(esysroot) + '/tools/CppUnitTest/inc'

esys_lib_path = str(esysroot) + '/lib'
esysUtils_lib_path = str(esysroot) + '/esysUtils/lib'
CppUnitTest_lib_path = str(esysroot) + '/tools/CppUnitTest/lib'

escript_lib = str(esys_lib_path) + '/libescriptcpp.so'

install_dir = str(esysroot) + '/escript/test'

cpp_path = [functionspace_test_path,
            escript_path,
            esysUtils_path,
            CppUnitTest_path,
            python_path,
            boost_path]

lib_path = [esys_lib_path,
            esysUtils_lib_path,
            CppUnitTest_lib_path,
            python_lib_path,
            boost_lib_path]

libs = ['escriptcpp',
        'esysUtils',
        'CppUnitTest',
        str(python_lib),
        str(boost_lib),
        'dl',
        'util']

libs.extend(sys_libs)

sources = ['FunctionSpaceTestCase.cpp',
           'FunctionSpaceTest.cpp']

target = 'FunctionSpaceTest.exe'

import os
functionspace_env = Environment(ENV = os.environ)

functionspace_env.Replace(CXX = cxx)
functionspace_env.Replace(CXXFLAGS = cxx_flags)
functionspace_env.Replace(CPPPATH = cpp_path)
functionspace_env.Replace(LIBPATH = lib_path)
functionspace_env.Replace(LIBS = libs)

functionspace_test_exe = functionspace_env.Program(target, sources)

Depends(functionspace_test_exe, escript_lib)

Alias("build_tests", functionspace_env.Install(install_dir, functionspace_test_exe))

import scons_extensions
runUnitTest_builder = Builder(action = scons_extensions.runUnitTest, suffix = '.passed', single_source=True)
functionspace_env.Append(BUILDERS = {'RunUnitTest' : runUnitTest_builder});

Alias("run_tests", functionspace_env.RunUnitTest(target))
